package base;

/**
 * Placeholder class in main sources to avoid pulling test framework into production code.
 * Renamed to avoid duplicate type with test BaseTest.
 */
class BaseTestPlaceholder {
    // intentionally empty placeholder for main sources
}